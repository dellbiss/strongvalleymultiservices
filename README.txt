STRONG VALLEY MULTI SERVICES — WEBSITE
======================================

FILES
-----
index.html    The full website (one page, mobile-first, SEO-optimized for Tampa)
robots.txt    Tells Google it can crawl the site
sitemap.xml   Helps Google index the site (with images + lastmod)
/images/      Your photos (optimized .jpg files the site uses)

=====================================================================
 DO THESE 4 THINGS TO GO FROM "BUILT" TO "MAKING MONEY" (in order)
=====================================================================

1. PLUG IN YOUR LEAD EMAIL  (2 min — REQUIRED, or the form won't send)
   - Open index.html, search for: REPLACE-WITH-YOUR-EMAIL
   - Replace it with the email where you want leads to land, e.g.
       action="https://formsubmit.co/yourname@email.com"
   - The FIRST time someone submits, FormSubmit emails you a one-click
     "activate" link. Click it once and every future lead hits your inbox.
   - No account, no cost. (Tell me your email and I'll drop it in for you.)

2. TURN ON LIVE GOOGLE REVIEWS  (built in — needs 2 values from you)
   The site now has a real Google reviews widget that pulls your live star
   rating + latest reviews straight from Google. To switch it on:
     a. Get a free Google Maps API key:
        https://console.cloud.google.com/google/maps-apis/credentials
        - Enable "Places API", create an API key.
        - Restrict it to "Websites": strongvalleyservices.com + dellbiss.github.io
     b. Get your Place ID:
        https://developers.google.com/maps/documentation/places/web-service/place-id
        (search your business name, copy the Place ID).
     c. In index.html: replace GOOGLE_PLACE_ID (in the script near the bottom)
        and GOOGLE_MAPS_API_KEY (in the maps loader at the very end), then
        delete the comment markers around that loader line so it runs.
   - Google gives $200/mo free Maps credit — far more than a small site uses.
   - Until configured, a clean Google-branded card + a "Read Our Google
     Reviews" button send visitors straight to your real Google profile.
   - Prefer no API key? A no-code alternative is trustindex.io or elfsight.com
     (free): connect Google, copy their embed, paste it over the
     "reviews-fallback" block. Either path works.

3. CREATE / CLAIM YOUR GOOGLE BUSINESS PROFILE  (#1 source of local cash)
   - https://business.google.com  — this is what puts you in the Google Map
     pack for "junk removal near me" / "pressure washing Tampa."
   - Use the SAME name, phone (844) 747-3075, and Tampa area as the site.
   - Add photos, your service areas, and ask happy customers for reviews.
   - Bigger lead driver than the website itself in the first few months.

4. GO LIVE + INDEX
   - Push to GitHub and enable Pages (or host on GoDaddy/Hostinger/Netlify).
   - Submit sitemap.xml in Google Search Console for fastest indexing.
   - Point your real domain (strongvalleyservices.com) at the host.

WHAT I JUST FIXED / ADDED
-------------------------
- Quote form now actually SENDS (FormSubmit) instead of just changing text.
  Added an email field so you can reply by email too. Spam honeypot included.
- Reviews are now a LIVE Google widget slot + a real button to your Google
  profile. Removed the old sample testimonials and the self-applied star
  rating from the code (Google penalizes self-made review snippets — your
  real stars will show through the widget instead).
- Real hero photo (your restored paver patio) instead of a flat gradient.
- Correct Facebook link + added Instagram, and linked your real social
  profiles (Facebook, Instagram, TikTok, Yelp) in the site's data for SEO.
- "Submit-on-success" thank-you banner after the form is sent.

WHAT'S OPTIMIZED FOR GOOGLE (TAMPA LOCAL SEO)
---------------------------------------------
- Business name "Strong Valley Multi Services" everywhere
- Targets Tampa + Brandon, Riverview, Plant City, Clearwater, St. Petersburg
- Keyword-rich title, meta, headings & a dedicated Service Area section
- LocalBusiness schema with geo coordinates, areaServed cities, real socials
- Service, FAQ & Breadcrumb structured data (rich-results eligible)
- Open Graph / Twitter cards with image alt text for clean social sharing
- All photos: web-optimized, keyword filenames, alt text, lazy-loaded, in sitemap

WHAT'S OPTIMIZED FOR MOBILE
---------------------------
- Mobile-first layout; everything stacks cleanly on phones
- Sticky bottom bar with BOTH "Call Now" and "Free Quote" buttons
- Tap targets >= 52px, safe-area padding for notched iPhones
- 16px form inputs (prevents iOS zoom-on-focus), tel/email/name autocomplete
- Drag-to-compare before/after sliders (touch + mouse + keyboard)
- Single-file load, no external fonts = very fast on cellular

NOTE ON "10/10"
---------------
The site itself is now built to convert. The remaining ceiling on results is
NOT the code — it's the 4 steps above (especially the lead email, Google
Business Profile, and going live). Those depend on your accounts, so they're
yours to click; everything technical is done.
